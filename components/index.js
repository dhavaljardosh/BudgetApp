import React, { Component } from "react";
import { View, Text } from "react-native";
import MainPage from "./MainPage";

export default () => {
  return (
    <View style={{ flex: 1 }}>
      <MainPage />
    </View>
  );
};
